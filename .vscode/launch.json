{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "build and start",
      "preLaunchTask": "build",
      "program": "${workspaceFolder}\\server\\twit-server.js"
    }
  ]
}
